import 'package:flutter/material.dart';

class PhoneAuthLogo extends StatelessWidget {
  const PhoneAuthLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.phone_android, size: 80, color: Color(0xFF4CAF50));
  }
}

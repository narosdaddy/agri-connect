import 'package:flutter/material.dart';

class EmailVerificationLogo extends StatelessWidget {
  const EmailVerificationLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.mark_email_read, size: 70, color: Colors.white);
  }
}
